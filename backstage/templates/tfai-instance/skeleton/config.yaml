# ${{ values.instanceName }} — TF-AI configuration
# Generated by Backstage scaffolder template.
#
# This is the primary configuration file. Secrets (API keys) go in .env.
# Environment variables override any value set here.

model:
  provider: ${{ values.modelProvider }}
  max_tokens: 4096
  temperature: 0.2

  ollama:
    host: http://localhost:11434
    model: llama3

  # openai:
  #   model: gpt-4o

  # azure:
  #   endpoint: https://your-resource.openai.azure.com
  #   deployment: gpt-4o
  #   api_version: "2025-04-01-preview"

  # bedrock:
  #   region: us-east-1
  #   model_id: ""

  # gemini:
  #   model: gemini-1.5-pro

embedding:
  provider: ${{ values.modelProvider }}
  # model: nomic-embed-text
  # dimensions: 768

qdrant:
  host: qdrant
  port: 6334
  collection: tfai-docs

server:
  host: 0.0.0.0
  port: ${{ values.serverPort }}

logging:
  level: info
  format: json

# tracing:
#   host: http://langfuse:3000
