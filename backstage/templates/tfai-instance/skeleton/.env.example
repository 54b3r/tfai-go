# ─── TF-AI Instance: ${{ values.instanceName }} ───────────────────────────────
# Secrets only — all non-secret config lives in config.yaml.
# Copy this file to .env and fill in your credentials.
# Environment variables override config.yaml values.

# ─── API Keys (never put these in config.yaml) ───────────────────────────────
# OPENAI_API_KEY=sk-...
# AZURE_OPENAI_API_KEY=
# GOOGLE_API_KEY=
# EMBEDDING_API_KEY=
# QDRANT_API_KEY=
# TFAI_API_KEY=              # set a strong token to enable API auth
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
